-- Active: 1788283797601@@127.0.0.1@5432@catalogo_filmes
CREATE TABLE IF NOT EXISTS "usuarios" (
	"id_usuario" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"nome" VARCHAR(150) NOT NULL,
	"email" VARCHAR(150) NOT NULL UNIQUE,
	"senha" VARCHAR(255) NOT NULL,
	"criado_em" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"atualizado_em" TIMESTAMP,
	PRIMARY KEY("id_usuario")
);

CREATE TABLE IF NOT EXISTS "filmes" (
	"id_filmes" INTEGER NOT NULL GENERATED BY DEFAULT AS IDENTITY,
	"id_usuario" INTEGER NOT NULL,
	"titulo" VARCHAR(200) NOT NULL,
	"ano_lancamento" DATE NOT NULL,
	"genero" VARCHAR(100) NOT NULL,
	"nota" DECIMAL(3,1) CHECK(nota >= 0 AND nota <= 10),
	"capa_url" TEXT,
	"criado_em" TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	"atualizado_em" TIMESTAMP,
	PRIMARY KEY("id_filmes")
);

ALTER TABLE "filmes"
ADD FOREIGN KEY("id_usuario") REFERENCES "usuarios"("id_usuario")
ON UPDATE NO ACTION ON DELETE NO ACTION;